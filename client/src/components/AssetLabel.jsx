import { Box, Typography, Button } from "@mui/material";
import { Image } from "mui-image";
import { useState } from "react";

const AssetLabel = ({ name, image, xpValue, onClick }) => {
    return (
        <Box></Box>
    )}

export default AssetLabel;
